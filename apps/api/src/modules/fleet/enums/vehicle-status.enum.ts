export enum VehicleStatus {
  /** En circulation, position récente. */
  ACTIVE = 'active',
  /** À l'arrêt / au terminus. */
  IDLE = 'idle',
  /** Pas de position récente. */
  OFFLINE = 'offline',
}
