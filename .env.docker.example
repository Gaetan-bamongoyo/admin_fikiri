# Copy to .env.docker for docker compose:
#   cp .env.docker.example .env.docker

# PostgreSQL (service: db)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=fikiri_traffic
POSTGRES_PORT=5432

# API (service: api)
API_PORT=7540
JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=7d
